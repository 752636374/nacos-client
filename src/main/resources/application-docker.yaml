spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.18.0.18:8848
        enabled: true
  redis:
    host: 172.18.0.8
    port: 6379
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 500
        min-idle: 0
    lettuce:
      shutdown-timeout: 0
logging:
  level:
    org:
      springframework:
        web: DEBUG